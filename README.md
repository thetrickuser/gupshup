# Ultra-Private P2P/Relay Chat — Monorepo (placeholder)

This repository will contain two top-level projects:
- /backend — Java 21 Spring Boot relay server (volatile queue)
- /frontend — Expo React Native TypeScript client

Current status: workspace scaffolding created with placeholder READMEs and .gitkeep files. Follow the Plan & Confirm Protocol in `.github/copilot-instructions.md` before initializing projects or making code changes.

See .github/copilot-instructions.md for full project directive and constraints.